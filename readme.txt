si inclure bibliothèque standard, mettre le fichier en argv[1]: 
   - g++ main.cpp && ./a.out bibliothequeStandard.txt testPointeurTab.txt | ./msm -d -d

//quand contante en majuscules ==> plante

//dup avant drop
//il faut get au lieu de push (dans addition)